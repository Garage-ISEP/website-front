export interface ModelLabs {
    id: number;
    name: string;
    image: HTMLImageElement;
    description: string;
  }
  
export interface Props {
    name: string;
    image: HTMLImageElement;
    description: string;
  }
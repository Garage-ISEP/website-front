export interface ModelLabs {
    id: number;
    name: string;
    image: string;
    description: string;
  }
  
export interface Props {
    name: string;
    image: string;
    description: string;
  }